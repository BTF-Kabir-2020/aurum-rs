# tests

Integration and unit tests that run with `cargo test`:

- indicator pipeline correctness (EMA / RSI / Momentum)
- signal engine rules
- provider payload parsing against fixtures
- symbol normalization
- database migrations
- backup validation / restore
- API responses

The full required test matrix lives in `README.md`, section 37.