# signal

Decision layer of the engine.

- `engine.rs` — rule-based `BUY` / `SELL` / `HOLD` from RSI + EMA + momentum
- Rule score / signal strength derived transparently from matched conditions

This is a rule engine for a demonstrator, not AI and not a trading recommendation.