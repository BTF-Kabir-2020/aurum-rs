# backups

Default destination for backups created with `aurum backup create`.

- Mounted into Docker at `/backups`
- Not committed to the repository

See `docs/BACKUPS.md` for the full backup/restore guide.